[build]
  command = "npm run build"
  publish = "dist"

# Environment variables (set in Netlify UI: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY)

# Multi-page app: no SPA catch-all redirect needed.
# Each page (login, register, assignments, etc.) has its own HTML file.
# Netlify serves them automatically from dist/src/pages/*.html
